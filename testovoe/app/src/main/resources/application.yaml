server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

spring:
  datasource:

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/changelog.xml
    default-schema: hotels

